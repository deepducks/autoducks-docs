---
import boardDark from '../../assets/home/board-cta-dark.png';
import boardLight from '../../assets/home/board-cta-light.png';

// Slides shown in the hero. The board slide uses the real screenshot; the
// GitHub and terminal slides are lightweight CSS mockups standing in for the
// eventual GIFs. Drop an <img> into their slide to swap in a real capture.
const slides = [
  { label: 'Trigger from a GitHub comment' },
  { label: 'Install from your terminal' },
  { label: 'Watch the board move' },
];
---

<div class="carousel" data-carousel>
  <div class="carousel__frame">
    <div class="carousel__bar" aria-hidden="true">
      <span class="carousel__light"></span>
      <span class="carousel__light"></span>
      <span class="carousel__light"></span>
    </div>

    <div class="carousel__viewport">
      <div class="carousel__slide is-active" data-slide role="group" aria-label={slides[0].label}>
        <div class="mock mock--github">
          <div class="mock__line">
            <span class="mock__avatar"></span>
            <span class="mock__meta"><strong>you</strong> commented</span>
          </div>
          <div class="mock__bubble"><code>/execute</code> model:opus effort:high</div>
          <div class="mock__line">
            <span class="mock__avatar mock__avatar--bot">🦆</span>
            <span class="mock__meta"><strong>autoducks</strong> started the pipeline</span>
          </div>
        </div>
      </div>

      <div class="carousel__slide" data-slide role="group" aria-label={slides[1].label}>
        <div class="mock mock--terminal">
          <p class="mock__cmd"><span class="mock__prompt">$</span> npx autoducks install</p>
          <p class="mock__out">✓ Wrote .autoducks + GitHub workflows</p>
          <p class="mock__out">✓ Configured 8 autos</p>
          <p class="mock__cmd"><span class="mock__prompt">$</span><span class="mock__cursor"></span></p>
        </div>
      </div>

      <div class="carousel__slide" data-slide role="group" aria-label={slides[2].label}>
        <img
          class="carousel__img carousel__img--dark"
          src={boardDark.src}
          width={boardDark.width}
          height={boardDark.height}
          alt="An autoducks board with example issues across pipeline columns"
          loading="lazy"
          decoding="async"
        />
        <img
          class="carousel__img carousel__img--light"
          src={boardLight.src}
          width={boardLight.width}
          height={boardLight.height}
          alt=""
          aria-hidden="true"
          loading="lazy"
          decoding="async"
        />
      </div>
    </div>
  </div>

  <div class="carousel__nav" role="tablist" aria-label="Hero preview">
    {
      slides.map((slide, index) => (
        <button
          class:list={['carousel__pip', { 'is-active': index === 0 }]}
          type="button"
          data-goto={index}
          role="tab"
          aria-label={slide.label}
          aria-current={index === 0 ? 'true' : 'false'}
        />
      ))
    }
  </div>
</div>

<script is:inline>
  (function () {
    var root = document.querySelector('[data-carousel]');
    if (!root) return;

    var slides = Array.prototype.slice.call(root.querySelectorAll('[data-slide]'));
    var pips = Array.prototype.slice.call(root.querySelectorAll('.carousel__pip'));
    if (!slides.length) return;

    var current = 0;
    var timer = null;
    var reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    function show(index) {
      current = (index + slides.length) % slides.length;
      slides.forEach(function (slide, i) {
        slide.classList.toggle('is-active', i === current);
      });
      pips.forEach(function (pip, i) {
        var active = i === current;
        pip.classList.toggle('is-active', active);
        pip.setAttribute('aria-current', active ? 'true' : 'false');
      });
    }

    function stop() {
      if (timer) {
        clearInterval(timer);
        timer = null;
      }
    }

    function start() {
      if (reduce) return;
      stop();
      timer = setInterval(function () {
        show(current + 1);
      }, 4200);
    }

    pips.forEach(function (pip, i) {
      pip.addEventListener('click', function () {
        show(i);
        start();
      });
    });

    root.addEventListener('mouseenter', stop);
    root.addEventListener('mouseleave', start);
    root.addEventListener('focusin', stop);
    root.addEventListener('focusout', start);

    show(0);
    start();
  })();
</script>

<style>
  .carousel {
    display: flex;
    flex-direction: column;
    gap: 0.9rem;
    width: 100%;
  }

  .carousel__frame {
    overflow: hidden;
    border: 1px solid var(--sl-color-gray-5);
    border-radius: 0.75rem;
    background: var(--sl-color-gray-6);
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.24);
  }

  .carousel__bar {
    display: flex;
    gap: 0.4rem;
    padding: 0.65rem 0.85rem;
    border-bottom: 1px solid var(--sl-color-gray-5);
    background: var(--sl-color-gray-7);
  }

  .carousel__light {
    width: 0.6rem;
    height: 0.6rem;
    border-radius: 999px;
    background: var(--sl-color-gray-4);
  }

  .carousel__viewport {
    position: relative;
    aspect-ratio: 16 / 10;
    background: var(--sl-color-gray-7);
  }

  .carousel__slide {
    position: absolute;
    inset: 0;
    display: flex;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.5s ease, visibility 0.5s ease;
  }

  .carousel__slide.is-active {
    opacity: 1;
    visibility: visible;
  }

  .carousel__img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  /* theme-aware board variant */
  .carousel__img--light {
    display: none;
  }
  :global(:root[data-theme='light']) .carousel__img--dark {
    display: none;
  }
  :global(:root[data-theme='light']) .carousel__img--light {
    display: block;
  }

  /* ── mockups ── */
  .mock {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 0.75rem;
    width: 100%;
    padding: 1.5rem clamp(1rem, 4%, 2rem);
    font-family: var(--__sl-font-mono);
    font-size: clamp(0.72rem, 0.6rem + 0.4vw, 0.9rem);
  }

  .mock--github {
    background: radial-gradient(120% 120% at 0% 0%, var(--sl-color-gray-6), var(--sl-color-gray-7));
  }

  .mock__line {
    display: flex;
    align-items: center;
    gap: 0.55rem;
    color: var(--sl-color-gray-2);
  }

  .mock__avatar {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.4rem;
    height: 1.4rem;
    border-radius: 999px;
    background: var(--sl-color-gray-4);
    font-size: 0.8rem;
  }

  .mock__avatar--bot {
    background: color-mix(in srgb, var(--sl-color-accent) 25%, transparent);
  }

  .mock__meta strong {
    color: var(--sl-color-white);
  }

  .mock__bubble {
    margin-left: 1.95rem;
    padding: 0.6rem 0.85rem;
    border: 1px solid var(--sl-color-gray-5);
    border-radius: 0.5rem;
    background: var(--sl-color-gray-6);
    color: var(--sl-color-gray-2);
  }

  .mock__bubble code {
    padding: 0.1rem 0.4rem;
    border-radius: 0.3rem;
    background: color-mix(in srgb, var(--sl-color-accent) 18%, transparent);
    color: var(--sl-color-accent-high);
    font: inherit;
  }

  .mock--terminal {
    gap: 0.4rem;
    background: var(--sl-color-black);
  }

  .mock__cmd {
    margin: 0;
    color: var(--sl-color-gray-1);
  }

  .mock__prompt {
    margin-right: 0.5rem;
    color: var(--sl-color-accent-high);
  }

  .mock__out {
    margin: 0;
    color: #3fb950;
  }

  .mock__cursor {
    display: inline-block;
    width: 0.5rem;
    height: 1rem;
    margin-left: 0.1rem;
    vertical-align: text-bottom;
    background: var(--sl-color-accent-high);
    animation: carousel-blink 1.1s steps(1) infinite;
  }

  @keyframes carousel-blink {
    50% {
      opacity: 0;
    }
  }

  /* ── nav pips ── */
  .carousel__nav {
    display: flex;
    justify-content: center;
    gap: 0.5rem;
  }

  .carousel__pip {
    width: 0.55rem;
    height: 0.55rem;
    padding: 0;
    border: none;
    border-radius: 999px;
    background: var(--sl-color-gray-4);
    cursor: pointer;
    transition: width 0.2s ease, background-color 0.2s ease;
  }

  .carousel__pip.is-active {
    width: 1.4rem;
    background: var(--sl-color-accent);
  }

  .carousel__pip:focus-visible {
    outline: 2px solid var(--sl-color-accent);
    outline-offset: 2px;
  }

  @media (prefers-reduced-motion: reduce) {
    .carousel__slide {
      transition: none;
    }

    .mock__cursor {
      animation: none;
    }

    .carousel__pip {
      transition: none;
    }
  }
</style>
